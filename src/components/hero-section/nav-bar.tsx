import { AppBar } from '@mui/material';
import * as React from 'react';

const NavBar: React.FC = () => {
    return (  
        <React.Fragment>
            <AppBar>
                
            </AppBar>
        </React.Fragment>
    );
}
 
export default NavBar;